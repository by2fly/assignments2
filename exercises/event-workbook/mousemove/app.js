const dockey = document.getElementById('red-box')
dockey.addEventListener('mouseover', function(x, y){
    const x = e.clientX;
    const y = e.clientY
    let coords = `x ${x} : y ${y}`;
const display = document.getElementById('coords').innerHTML =coords
}


)